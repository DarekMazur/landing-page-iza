import styled from 'styled-components';

export const StyledSocials = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 2rem 0;
    z-index: 2;
`